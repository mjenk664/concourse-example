- !host concourse

- !policy
  id: concourse
  owner: !host concourse      # Set explicit ownership to read and update all of the secrets within the Main team
  body:
  - !policy
    id: main   # concourse TEAM_NAME
    body:
      - &variables
        - !variable my-conjur-variable
        - !variable opsman_username
        - !variable git-private-key